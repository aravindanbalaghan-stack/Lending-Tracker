import PendingClient from "@/components/PendingClient";

export default function PendingPage() {
  return (
    <main className="min-h-screen flex items-center justify-center px-4 bg-paper">
      <PendingClient />
    </main>
  );
}
